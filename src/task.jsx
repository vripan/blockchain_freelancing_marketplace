import { HStack, OutlinedButton, VStack } from './styles';
import { useEffect, useState } from 'react';
import Contracts from './contracts';

export default function CreateTask({ walletAddress }) {
    let { memberManager } = Contracts;

    return (
    );
}